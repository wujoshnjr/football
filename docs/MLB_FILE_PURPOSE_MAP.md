# MLB File Purpose Map

This map covers important files actually read from `wujoshnjr/mlb-prediction-app`. It is a migration aid, not a full repository tree dump.

| File path | Category | Purpose | Inputs | Outputs | Key functions/classes | Dependencies | Downstream users | Reusable for football | Football equivalent | Migration priority | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `README.md` | project docs | Defines project position, safety, reports, workflow, and local commands. | Human project state. | Documentation. | N/A | N/A | Codex, maintainers, users. | yes | `README.md`, `PROJECT_CONTEXT.md`, `DATA_SOURCES.md`, `MODEL_CARD.md` | P0 | Strong source for governance language and workflow intent. |
| `main.py` | dashboard/API | FastAPI dashboard and report-driven API surface. | JSON/CSV reports, prediction module, sample state, finalized results. | HTML and JSON responses. | FastAPI app, report loaders, dashboard payload helpers. | FastAPI, pandas, sklearn, local reports. | Browser dashboard, API callers. | partial | Football dashboard/API with source health, fixtures, evidence, market consensus. | P2 | Keep report-driven design; adapt baseball UI/data semantics to World Cup. |
| `model.py` | data collection/model input | Unified upstream data collection and normalization. | MLB StatsAPI, statcast, retrosheet, pybaseball, sportsipy, weather, odds, injuries, bullpen, platoon, umpire. | `report/{date}.json`, normalized records, error list. | `UnifiedSportsModel`, `gather_all_data`, `safe_call`, `attach_schedule_game_ids`. | pandas, optional clients. | `prediction.py`. | partial | Football source registry / fixture-context ingestion service. | P1 | Use fail-soft optional source pattern, but replace MLB-specific sources. |
| `prediction.py` | prediction pipeline | Generates daily prediction report with governance and artifact checks. | `UnifiedSportsModel`, feature schema, risk guard, snapshot store, market store, model artifacts. | `report/prediction.json`, snapshots, diagnostics. | `generate_predictions`, model artifact gate helpers, JSON-safe helpers. | numpy, pandas, config, model, scripts. | Dashboard, reports, contract validator. | partial | Football 1X2 prediction pipeline and evidence payload. | P3 | Must adapt binary MLB to football home/draw/away and knockout advance_result. |
| `config.py` | runtime config | Central feature gates, model flags, sample thresholds, pipeline/snapshot policy. | Static config and env-adjacent runtime use. | Constants. | `PIPELINE_VERSION`, `SNAPSHOT_POLICY`, `MIN_CLEAN_TRAIN_SAMPLES`, feature flags. | N/A | `prediction.py`, snapshot/model scripts. | yes | Football config or settings additions. | P0 | Feature flags default conservative. Do not enable betting modes. |
| `requirements.txt` | dependency manifest | Python dependency list. | N/A | Installable environment. | N/A | FastAPI, pandas, sklearn, pytest, ML libs. | CI/local setup. | partial | Backend/project requirements. | later | Football should avoid unnecessary MLB-only heavy deps unless needed. |
| `scripts/feature_schema.py` | feature governance | Single source of truth for feature groups and feature schema hash. | Static feature lists. | Feature constants and hash. | `CORE_MODEL_FEATURES`, `TRACKING_ONLY_FEATURES`, `AVAILABILITY_FLAG_FEATURES`, `SHADOW_CANDIDATE_FEATURES`, schema hash helpers. | hashlib, json. | Prediction, training, reports, tests. | yes | `scripts/football_feature_schema.py` | P1 | Direct governance pattern, football-specific feature names. |
| `scripts/risk_guard.py` | safety/risk gate | Rejects unsafe or low-quality market candidates. | Market research report, candidate context, features. | Pass/reject tuple and reason. | `LiveBetRiskGuard`, `validate_bet_candidate`. | json, math. | `prediction.py`. | partial | Football safety policy / paper-only guard. | P0 | Must be adapted away from betting candidate approval toward safety rejection. |
| `scripts/snapshot_store.py` | snapshot store | Saves first valid pregame snapshots and settles results without recomputing features. | Prediction rows, kickoff time, finalized outcomes. | `data/prediction_snapshots.csv`. | `append_first_seen_pregame_snapshots`, settlement helpers, `SNAPSHOT_COLUMNS`. | pandas, csv, config, feature schema. | Sample state, evaluation, baseline comparison. | yes | `scripts/football_snapshot_store.py` | P2 | Strong pattern. Add 1X2 and knockout fields. |
| `scripts/data_contract_validator.py` | data contract | Validates required reports, JSON safety, statuses, and safety flags. | Reports and data artifacts. | `report/data_contract_report.json`. | `_validate_base_report`, `_validate_prediction`, JSON/safety validators. | json, pathlib. | CI, promotion gate, dashboard. | yes | `scripts/football_data_contract_validator.py` | P1 | Extend with source report schema, forbidden football fields, API key scan. |
| `scripts/pipeline_manifest.py` | artifact inventory | Tracks many report/data/site/docs artifacts with hashes and summaries. | Filesystem artifacts. | `report/pipeline_manifest.json`. | `_sha256`, `_json_summary`, `_csv_row_count`, manifest builder. | csv, hashlib, json. | CI, promotion gate, health dashboard. | yes | `scripts/football_pipeline_manifest.py` | P2 | Use smaller football artifact list first. |
| `scripts/promotion_gate.py` | promotion gate | Blocks promotion based on samples, model-vs-market, CLV, calibration, research quality, data contract. | Sample state, evaluation, market, data contract reports. | `report/promotion_gate_report.json`. | `build_report`, blockers logic. | json, pathlib. | CI, dashboard, model governance. | partial | Football promotion gate / artifact gate. | P3 | Use football sample thresholds and multiclass metrics. |
| `scripts/calibration_report.py` | evaluation | Builds calibration report from OOS predictions. | `data/oos_predictions_with_labels.csv`. | `report/calibration_report.json`. | `generate_report`, `calibration_bins`, JSON-safe writer. | numpy, pandas. | Promotion gate, dashboard. | partial | `scripts/football_calibration_report.py` | P3 | Convert binary calibration to home/draw/away multiclass bins. |
| `scripts/sample_state_builder.py` | sample governance | Builds sample counts and readiness from snapshots/finalized outcomes/training state. | Snapshot CSV, finalized CSV, model status, reports. | `data/sample_state.json`, `report/sample_state_report.json`. | sample counters, training readiness helpers. | pandas, numpy. | Promotion gate, README dashboard. | yes | Football sample state builder. | P2 | Add group/knockout and clean pregame distinctions. |
| `scripts/baseline_comparison_report.py` | model vs market | Compares model probabilities with constant, historical, Elo, and market no-vig baselines. | Prediction report, snapshots, finalized games, market odds. | `report/baseline_comparison_report.json`. | Brier, logloss, no-vig helpers. | pandas, numpy. | Promotion gate, dashboard. | partial | Football model-vs-market 1X2 report. | P3 | Convert binary metrics to multiclass home/draw/away. |
| `scripts/market_close_report.py` | CLV / market close | Links snapshots to closing market odds and computes market movement evidence. | Prediction snapshots, market odds history. | `report/market_close_report.json`. | market odds grouping, selected side, CLV helpers. | pandas. | Promotion gate, research reports. | partial | Football market movement / paper tracking report. | P3 | Keep evidence-only, no betting advice. |
| `scripts/mlb_stats_client.py` | source client | Fetches MLB schedule and basic game context from StatsAPI. | MLB StatsAPI schedule endpoint. | pandas DataFrame. | `fetch_mlb_statsapi`. | requests, pandas. | `model.py`. | partial | Football fixture adapters. | P1 | Pattern is simple but lacks standardized report; football should wrap with SourceReport. |
| `scripts/odds_client.py` | market source client | Fetches odds, redacts API key in diagnostics, classifies market quality, saves snapshots. | Odds API key from env, odds endpoint. | DataFrame rows, diagnostic JSON, odds snapshots. | `fetch_odds`, `_base_diagnostic`, `_redact_params`, quality helpers. | requests, pandas, numpy. | `model.py`, market reports. | partial | Football market consensus adapter/report. | P2 | Keep redaction and quality diagnostics; no write/execution behavior. |
| `scripts/sanitize_prediction_report.py` | report sanitizer | Removes NaN/Infinity-like values from prediction report. | `report/prediction.json`. | sanitized prediction report and sanitization report. | `sanitize_prediction_report`. | json, math. | CI/data contract. | yes | Football report sanitizer or data contract helper. | P2 | Useful before dashboard and data contract validation. |
| `scripts/html_report_builder.py` | dashboard export | Builds static report/dashboard HTML from public dashboard data. | `site/data/public_dashboard.json` or builder function. | `report/index.html`. | `_load_dashboard`, card builders, safe formatters. | json, html. | CI/static dashboard. | partial | Football dashboard export. | later | Football frontend already uses Next.js; artifact-driven payload still useful. |
| `scripts/model_artifact_status.py` | artifact gate | Checks model artifact presence, metadata, feature schema hash, sample count, and active eligibility. | Model artifact, training samples/status, feature schema. | `data/model_artifact_status.json`, `report/model_artifact_status_report.json`. | artifact loader, metadata extraction, sample count checks. | joblib, pickle, csv. | Prediction gate, data contract, promotion gate. | yes | Football model artifact status. | P3 | Essential before trained model claims. |

## Important Unresolved File Discovery

GitHub Actions workflow path was not discovered through tried connector paths. The README documents a scheduled workflow, so Phase 13 must re-check actual workflow files before editing CI.

Test files were not discovered through direct path attempts in this phase. Phase 2 should inventory the football repo's tests; Phase 1 evidence still shows pytest is a declared dependency in the MLB repo.
